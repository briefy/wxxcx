const isEmpty = require('./is-empty');
const debug = require('./debug');
const setifyAndGetify = require('./setify-and-getify');
const merge = require('./merge');
const formatTime = require('./format-time');

module.exports = {
  isEmpty,
  debug,
  setifyAndGetify,
  merge,
  formatTime
}
